/* Restricted (QuickJS) host entry: the Host executes setup(serpent) and
   dispose() — the view UI itself lives in the sandboxed iframe page. */
async function setup() {}

async function dispose() {}

void setup;
void dispose;
