# Menu Command Probe

Fixed standard plugin fixture for PLUGIN-009 / PLUGIN-010 / PLUGIN-012. Registers asset, folder, collection menu commands and a workspace toolbar command; writes invoked context ids to plugin storage.
