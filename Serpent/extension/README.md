# Serpent Chromium 扩展

该扩展支持在 Chrome、Edge 等 Chromium 浏览器中右键网页图片或视频，并将媒体保存到正在运行的 Serpent 桌面应用。

媒体由**浏览器侧下载**（携带 Cookie 与页面 Referer），再上传到本机 Serpent，可绕过多数防盗链限制。只要 Serpent 在运行且已打开资源库，扩展即可连接（本机 127.0.0.1）。

## 构建

在 Serpent 项目根目录运行：

```bash
npm run extension:build
```

可安装产物生成在 `dist/extension/`。该目录包含编译后的 JavaScript、Manifest 和 PNG 图标；不要直接加载源码目录 `extension/`。

## 手动安装

1. 先完成上述构建。
2. Chrome 打开 `chrome://extensions`；Edge 打开 `edge://extensions`。
3. 开启“开发者模式”。
4. 点击“加载已解压的扩展程序”，选择 `dist/extension/`。

## 使用

1. 启动 Serpent 并打开一个资源库。
2. 扩展工具栏图标在已连接时会变为彩色；未连接时保持灰色。
3. 在网页图片或视频上右键，选择「保存到 Serpent」，再选择「根目录」或目标文件夹。
4. **拖拽图片时自动展开径向保存轮盘（Hotbox）**：朝一个方向甩动选择扇区，在扇区上松开即保存到该位置；在可展开项上继续外甩穿越外环可进入下一级（「‹ 返回」扇区固定在来向正对面，松开或穿越均可返回）；中心 ✕ 或环外松开、按 Esc 均退出不保存。根级 12 点起为最近保存位置。交互规格见 `docs/internal/ui/0002-extension-drag-radial-save-menu.md`。

拖拽解析会优先使用浏览器提供的媒体元素/拖拽数据，再回退到坐标命中，因此链接、遮罩、懒加载容器和 CSS 背景图也能沿用右键保存的媒体地址。要在 Chrome/Edge 中拖拽或右键保存本地 `file://` 图片，请在扩展详情页打开“允许访问文件网址”；本地图片会在页面内转换为 PNG 上传，不读取或暴露本地路径。
5. Pinterest、Behance、Google 图片等站点与博客一样，使用浏览器原生右键菜单中的「保存到 Serpent」子菜单（不再用浮层替换整站右键菜单）；拖拽图片仍可展开径向保存轮盘。

扩展会通知保存请求已接收、Serpent 未运行、浏览器下载失败，或桌面应用给出的具体拒绝原因。最近使用过的文件夹会排在子菜单前面并占据轮盘根级黄金位置。Serpent 客户端会对扩展保存执行与本地导入相同的内容查重。MVP 不会自动启动桌面应用。

桌面应用内打开「设置 → 常规」可查看安装说明。正式发布前安装文案会再改写（工单 `Serpent-999o`）。

扩展选项页可关闭 Windows 系统通知（保存成功/失败仍可在网页浮层菜单看到结果），也可关闭「拖拽网页图片/视频时显示保存轮盘」（关闭后拖拽保持完全原生行为）。
