/**
 * Browse-scope fetch limits (Serpent-6w7n).
 * One query returns every lightweight row for the current scope; thumbnails
 * and heavy metadata stay on-demand.
 */
export const BROWSE_SCOPE_MAX_ASSETS = 50_000;
