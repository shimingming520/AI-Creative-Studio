import WebSocket from 'ws';
const sleep=ms=>new Promise(r=>setTimeout(r,ms)); const ts=await (await fetch('http://127.0.0.1:9223/json/list')).json(); const t=ts.find(x=>x.type==='page'&&/serpentHosted/.test(x.url)); const ws=new WebSocket(t.webSocketDebuggerUrl,{maxPayload:128*1024*1024}); await new Promise((r,j)=>{ws.on('open',r);ws.on('error',j)}); let id=0,p=new Map(); ws.on('message',d=>{const m=JSON.parse(d);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}}); const ev=async e=>{const r=await new Promise((r,j)=>{const i=++id;p.set(i,m=>m.error?j(m.error):r(m.result));ws.send(JSON.stringify({id:i,method:'Runtime.evaluate',params:{expression:e,awaitPromise:true,returnByValue:true}}))});return r?.result?.value};
console.log(await ev(`(()=>({v2:!!document.querySelector('#v2-wrap'),text:document.querySelector('#v2-wrap')?.textContent?.slice(0,500),cards:document.querySelectorAll('#v2-wrap [class*=shot]').length,images:document.querySelectorAll('#v2-wrap img').length}))()`));
await ev(`(()=>{const b=[...document.querySelectorAll('#v2-wrap button')].find(b=>/素材设定/.test(b.textContent));b?.click();return !!b})()`); await sleep(4000);
console.log(await ev(`(()=>({text:document.querySelector('#v2-wrap')?.textContent?.slice(0,1800),cards:document.querySelectorAll('#v2-wrap [class*=shot]').length,images:document.querySelectorAll('#v2-wrap img').length}))()`)); ws.close();
