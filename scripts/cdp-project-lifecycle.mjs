import WebSocket from 'ws';
const sleep=ms=>new Promise(r=>setTimeout(r,ms)); const ts=await (await fetch('http://127.0.0.1:9223/json/list')).json(); const t=ts.find(x=>x.type==='page'&&/serpentHosted/.test(x.url)); const ws=new WebSocket(t.webSocketDebuggerUrl); await new Promise((r,j)=>{ws.on('open',r);ws.on('error',j)}); let id=0,p=new Map(); ws.on('message',d=>{const m=JSON.parse(d);if(m.id&&p.has(m.id)){p.get(m.id)(m);p.delete(m.id)}}); const ev=async e=>{const r=await new Promise((r,j)=>{const i=++id;p.set(i,m=>m.error?j(m.error):r(m.result));ws.send(JSON.stringify({id:i,method:'Runtime.evaluate',params:{expression:e,awaitPromise:true,returnByValue:true}}))});return r?.result?.value};
console.log('back',await ev(`(()=>{const b=document.querySelector('[data-person-replacement-action="back-home"]');b?.click();return !!b})()`)); await sleep(800);
console.log('home',await ev(`(()=>({text:document.querySelector('#v2-wrap')?.textContent?.slice(0,1200),cards:[...document.querySelectorAll('#v2-wrap [data-person-replacement-project-id],#v2-wrap .person-replacement-project-card')].map(x=>({text:x.textContent.trim().slice(0,80),id:x.getAttribute('data-person-replacement-project-id')})),buttons:[...document.querySelectorAll('#v2-wrap button')].map(x=>x.textContent.trim()).filter(Boolean).slice(-20)}))()`));
ws.close();
